[package]
name = "graph-population"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
ron = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
graph-population-fixture = { path = "../graph-population-fixture" }
